# Plugins for local-development screenshot runs. NOT used by CI - the
# reusable GitHub Actions workflow takes the plugin via inputs from
# the calling repo. See README.md for the CI flow.

plugins:
  - id: discourse-itinerary
    name: Itinerary
    source:
      type: local
      path: /home/ducks/dev/discourse-itinerary
