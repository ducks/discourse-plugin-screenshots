# List of plugins to screenshot. Each entry points at a local path or
# git URL; the tool reads each plugin's own config/screenshots.yml to
# know what to capture.

plugins:
  - id: discourse-itinerary
    name: Itinerary
    source:
      type: local
      path: /home/ducks/dev/discourse-itinerary
